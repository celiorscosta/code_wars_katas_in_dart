List<int> invert(List<int> arr) {
  return arr.map((element) => -element).toList();
  var a = ' ss s ';

  a.trim();
  // var list = <int>[];
  // arr.forEach((element) {
  //   return list.add(element - element - element);
  // });

  // return list;
}
